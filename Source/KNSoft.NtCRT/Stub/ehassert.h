#pragma once

#include <stdlib.h>

/* TODO */

#define EHTRACE_FMT1(fmt, p1)
#define EHTRACE_FMT2(fmt, p1, p2)
#define EHTRACE_HANDLER_EXIT(ExceptionDisposition)

#define _VCRT_VERIFY(cond) if (!(cond)) abort();
